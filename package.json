{
  "name": "print-stocks-serverless",
  "version": "0.0.1",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "deploy": "tsc && sls deploy --verbose",
    "invoke": "sls invoke -f printStocks -l",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "puppeteer": "^13.7.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "aws-sdk": "^2.1157.0",
    "chrome-aws-lambda": "^10.1.0",
    "puppeteer-core": "^13.7.0"
  },
  "overrides": {
    "puppeteer-core": "^13.7.0"
  }
}
